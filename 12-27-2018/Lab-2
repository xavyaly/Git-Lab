# PULL THE LATEST CHANGES FROM DIFFERENT BRANCHES
$ git init
$ git branch
$ pwd
$ ls -l
$ nano <file-name>
$ git status
$ git add .
$ git status
$ git commit -m “MSG”
$ git push						: ERROR, You haven’t pull the latest changes from Remote to Local
$ git pull origin <branch-name>		: communicate with the developers to check the specific branch
$ ls -l
$ git add .
$ git status
$ git commit -m “MSG”
$ git push origin <current-branch-name>		: push the changes from local to remote
OR
$ git push origin <current-branch-name> —force	: push the changes from local to remote forcefully
